services:
  backend:
    build: .
    container_name: go-backend
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      DATABASE_PATH: ${DATABASE_PATH}
    restart: unless-stopped